/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'akcdn.detik.net.id',
      'assets.promediateknologi.id',
      'tourbandung.id',
    ],
  },
};

module.exports = nextConfig;
